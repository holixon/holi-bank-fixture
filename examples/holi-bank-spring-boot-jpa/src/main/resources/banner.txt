-------------------------------------------------------------------------------
  Holibank JPA example application, Spring Boot ${spring-boot.version}
-------------------------------------------------------------------------------

  REST Console: http://localhost:${server.port}${springdoc.swagger-ui.path}
    H2 Console: http://localhost:${server.port}${spring.h2.console.path}
